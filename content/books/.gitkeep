# Bookshelf entries live here as `*.mdx`.
# Required frontmatter: title, author, cover (optional: summary).
# This file keeps the directory in git so the content-collections build finds it when empty.
